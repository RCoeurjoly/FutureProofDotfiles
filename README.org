nix run . -- --load init.el

To install a GDM session entry for Home Manager i3 on Ubuntu:

sudo ./scripts/install-gdm-i3-session.sh roland

To install the persistent YPCB Digilent FT232H udev rule:
  cd /home/roland/FutureProofDotfiles
  sudo ./scripts/install-ypcb-udev-rule.sh
